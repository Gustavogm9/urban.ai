# Output Examples

## Example 1: Roadmap Status Updated
```markdown
### Sprint 4
- [x] Integrate Stripe Webhooks (Validated via commit `a1b2c3`)
- [ ] Render dynamic UI
```

## Example 2: Technical Doc Update
```markdown
## Stripe Webhooks
The system now listens to `invoice.paid` and automatically grants tokens to the user entity. Handled in `stripe.controller.ts`.
```
